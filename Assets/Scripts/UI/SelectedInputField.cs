using UnityEngine;
using UnityEngine.EventSystems;

namespace UI
{
    public class SelectedInputField : MonoBehaviour
    {
        private void Start()
        {
//            EventSystem.current.SetSelectedGameObject(transform.gameObject);
        }
    }
}
